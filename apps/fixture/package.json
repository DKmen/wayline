{
  "name": "@wayline/fixture",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 4300 --strictPort",
    "build": "vite build",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "vite": "^8.1.4",
    "@playwright/test": "^1.61.1"
  }
}
